export enum ROLE_TYPE {
  MANAGER = 'MANAGER',
  EMPLOYEE = 'EMPLOYEE',
  HR = 'HR',
}

export enum ABSENT_REQUEST {
  PENDING = 'PENDING',
  NO = 'NO',
  YES = 'YES',
}

export type Language = 'en-US' | 'sl-SI';
